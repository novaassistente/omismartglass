# Name,   Type, SubType, Offset,   Size,    Flags
# OTA partition table for OmiGlass with 8MB flash + LittleFS chunk store (S2).
#
# NVS pinned at 0x9000 — do NOT move, wipes provisioned wifi creds.
# Moving the nvs row corrupts the pn_wifi namespace flashed by
# scripts/provision_nvs.py and bricks the pendant (premortem S2-P1).
#
# The 'spiffs' subtype on the littlefs row is intentional — arduino-esp32's
# LittleFS library reads partition entries whose subtype == data/spiffs and
# the physical filesystem format is selected by the mount call, not the CSV.
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x180000,
app1,     app,  ota_1,   0x190000, 0x180000,
littlefs, data, spiffs,  0x310000, 0x4F0000,
